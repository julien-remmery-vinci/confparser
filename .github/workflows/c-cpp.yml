name: C/C++ CI

on:
  push:
    branches: 
      - '**'
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build for testing
      run: make test

    - name: Test with valgrind
      run: make valgrind
